{
  "name": "Terraform Lab",
  "build": {
    "dockerfile": "Dockerfile"
  },

  "mounts": [
    "source=/var/run/docker.sock,target=/var/run/docker.sock,type=bind"
  ],

  "runArgs": [
    "--group-add",
    "docker"
  ],
 "postCreateCommand": "sudo groupmod -g $(stat -c '%g' /var/run/docker.sock) docker || true && sudo usermod -aG docker vscode",
  "customizations": {
    "vscode": {
      "extensions": [
        "hashicorp.terraform"
      ]
    }
  }
}